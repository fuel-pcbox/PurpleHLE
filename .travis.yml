language: cpp
sudo: required
dist: xenial
compiler:
- gcc
os:
- linux

before_script:
  - sudo apt-get update
  - sudo apt-get install zlib1g zlib1g-dev libminizip-dev minizip libminizip1
  - cmake --version
  - mkdir build
  - cd build
  - cmake ..

script: make
